import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        flex: 1,
    },
    titleContainer: {
        marginTop: 50,
        marginBottom: 'auto',
        alignItems: 'center',
    },
    title: {
        fontSize: 32,
        color: '#45403fff',
        fontFamily: 'DancingScript',
    }
})