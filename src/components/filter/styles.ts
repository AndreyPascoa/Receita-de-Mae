import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        height: 50,
        width: '47%',
        padding: 5,
        margin: 5,
        borderRadius: 50,
        justifyContent: 'center',
        alignItems: 'center',
        elevation: 1,
        flexDirection: 'row',
        gap: 5,
    }
})